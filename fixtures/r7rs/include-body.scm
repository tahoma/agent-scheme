;;; Include-body fixture for Agent Scheme library include tests.
;;;
;;; This file is read only when policy explicitly allows fixture include paths.

;; Define the value imported by include and load policy tests.
(define answer (+ 40 2))
