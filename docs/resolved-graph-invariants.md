# Resolved Graph Invariants

Roadmap dependency-graph invariants (from
[tahoma/agent-scheme#53](https://github.com/tahoma/agent-scheme/issues/53))
whose referenced issues are **all closed** are archived here — mirroring the
migration of completed chunks 0.00-0.14 into `release-notes.md`. They are kept for
historical and dependency-rationale reference; the live roadmap issue retains only
invariants with at least one open referenced issue, plus standing structural rules.

Migrated 2026-05-31 (under issue #376 / PR #377).

## Invariants

- Roadmap metadata cleanup (#295) should keep labels, dependency metadata, and GitHub sub-issue relationships aligned with the taxonomy and issue prose without changing implementation scope.
- Roadmap documentation refresh (#294) should follow #53 and keep `docs/roadmap.md` as a concise summary, not a duplicate source of truth.
- Unit test harness (#62) should precede conformance fixtures (#12) and implementation tickets such as tahoma/agent-scheme#2.
- GitHub Actions (#69) should continue to run the same `make test` command as the local harness.
- CI test splitting and timing reports (#322) should follow #69, keep `make test` as the local aggregate command, and make the Emacs-hosted and portable Chibi-backed validation paths visible as first-class CI shards before later test-suite growth hides their relative cost.
- CI shard rebalancing (#325) should follow #322 and wait for at least five CI runs with shard timing summaries so the second-pass split is based on observed wall time, ERT time, result counts, skip counts, and slowest-test data rather than a one-off local estimate.
- Docstring retention modes (#358) should follow #300, #301, #303, #344, and #325 so runtime retention tradeoffs are handled separately from CI shard rebalancing while preserving the default documentation reflection surface.
- Gambit host-compiled executable work (#273) should follow #325 and the initial `make compile` packaging scaffolding (#270), build on the interpreted Gambit host signal from #271, keep generated `gsc` artifacts under the build tree, and stay focused on Gambit compile/run coverage before broader executable variants.
- Getting started documentation (#264) should stay aligned with the native Emacs REPL UX (#20), development setup, and non-Emacs host status; it should describe planned host contracts as planned rather than usable first-use paths.
- Shared base prelude work (#73) should follow the first `(scheme base)` slice (#4) and stay distinct from full standard-library completion (#19).
- Shared fixture corpus (#92) should follow the unit harness (#62), conformance matrix (#12), reader (#2), and evaluator kernel (#3), then precede oracle, mining, and coverage-expansion work such as tahoma/agent-scheme#93, tahoma/agent-scheme#94, and tahoma/agent-scheme#95.
- Reference oracle work (#93) should come before broad external-suite mining (#94) when mined cases need comparison against available implementations.
- Gambit host parity (#271) should follow #93 and establish optional R7RS host behavior before Gambit executable packaging (#273).
- Conformance coverage audit (#95) should consume mined cases from #94 where useful and should keep policy-gated, unspecified, variant, and Agent-specific cases explicit.
- Inline implementation documentation (#77) should wait until the macro expansion surface (#13) has settled enough to avoid immediate comment churn.
- Scheme source documentation rules (#96) should complement #77 and settle before large module refactors such as tahoma/agent-scheme#100.
- Existing Scheme source documentation application (#106) should follow #96 and stay related to inline implementation documentation (#77) without changing runtime behavior.
- Pass-oriented architecture (#97) should precede primitive/effect metadata (#98), evaluator module refactors (#100), and backend effect contracts such as tahoma/agent-scheme#102 and tahoma/agent-scheme#103.
- Primitive/effect metadata (#98) should preserve the kernel versus portable-library boundary and inform derived-library migration (#101), evaluator refactors (#100), and backend effect paths (#103).
- Embedded portable library extraction (#99) and derived portable-library migration (#101) should preserve the library/import semantics from #14 and the base prelude split from #73.
- Policy/audit (#7) should precede mutating capabilities such as tahoma/agent-scheme#6 and tahoma/agent-scheme#8.
- Capability grants (#43) should follow transactional buffer edits (#6), programmable approvals (#30), and session lifecycle (#41) so grant attenuation has concrete capability and session records to constrain.
- Session lifecycle (#41) should precede the user-facing REPL UX (#20), memory scopes (#22), and jobs (#46).
- Policy-gated `(scheme repl)` interaction environment (#313) should follow #18, #19, #20, #41, and #102 so `interaction-environment` can expose only the current authorized session environment while preserving default denial outside a session.
- Capability environment design (#102) should consolidate after policy/audit (#7), session lifecycle (#41), grants (#43), and secrets/redaction (#49), while staying consistent with multi-host boundaries (#55) and pass architecture (#97).
- File and port capability domains (#220 and #221) should follow #102 and preserve the shared request, decision, revocation, handle, and audit vocabulary before later backend effect routing depends on them.
- Process capability work (#222) should follow #102 and the controlled command/window capability surface (#8), and should route process-backed ports through the port capability domain (#221).
- Policy-gated `(scheme time)` clock bindings (#311) should follow #18, #19, #102, and #103 so `current-second`, `current-jiffy`, and `jiffies-per-second` become shared capability requests instead of direct host clock reads or permanent deny-only stubs.
- Network capability work (#290) should follow #102, #103, #49, and #221, keep HTTP/SSE/provider/Hub/forge transport behind explicit authority, and precede provider proof fixtures (#289) and outward protocol implementation that performs network effects.
- The Emacs CLI-compatible host affordance subset (#254) should follow Emacs opaque handles (#5), policy (#7), approvals (#30), sessions (#41), capability domains (#220, #221, #222), Emacs host-adapter declaration (#234), and the host adapter conformance suite (#237); it should claim only the CLI/daemon affordances that Emacs can satisfy with matching Scheme-visible records and authorization behavior.
- Provider capability work (#223) should follow #102, model provider routing (#26), and secrets/redaction (#49), and should keep provider streams aligned with the port capability domain (#221).
- Shared backend effect path (#103) should follow #102, #97, and #98 before future compiler backends implement host effects.
- Shared VCS capability contract (#266) should follow the shared capability environment (#102) and shared backend effect path (#103), define host-neutral VCS records and Git parsers, and precede Emacs VCS (#36), policy-gated VCS mutation (#279), and future host VCS adapters such as native CLI/daemon VCS (#280).
- Policy-gated mutating VCS operations (#279) should follow the shared VCS contract (#266), preserve the read-only versus mutation split, and route repository-changing actions through explicit authority, policy, and audit records.
- Emacs mutating VCS adapter work (#292) should follow #279, the read-only Emacs VCS adapter (#36), process capabilities (#222), capability mediation conformance (#244), and redaction policy (#49).
- Normalize-to-LLIR lowering (#116) should follow #115 and the evaluator/module boundary work in #100.
- Compiled effects (#121) should follow #98, #102, #103, #115, #120, and #336 so native backends share the same policy-gated effect path as the interpreter without losing policy-visible records across collection points.
- The Emacs Lisp byte-code backend contract (#123) should follow #115, #120, and evaluator/module boundary work in #100 so it consumes LLIR instead of reimplementing frontend responsibilities.
- Model provider routing (#26) landed with the workflow foundation layer and should precede task/control-loop provider use (#281 and #289), Hugging Face bootstrap profiles (#25), and provider capability transport (#223).
- Project development skill bundle (#131) can proceed before Agent Skills interop (#38), native skill manifests (#40), and export (#39); later runtime work may reuse the bundle as examples without making it a prerequisite.
- User-facing custom library reference docs (#284) should follow `define-library` and imports (#14) and package/library resolution (#50), stay aligned with the R7RS reference library (#24), and document public Agent Scheme custom libraries separately from internal `(agent-scheme ...)` implementation modules, R7RS-small standard libraries, and optional SRFI compatibility libraries.
- Syntax datum source metadata (#338) should follow macro expansion introspection (#45), stay aligned with reader, macro, and library frontend boundaries (#2, #13, #14, and #97), and inform later LLIR/source mapping and documentation metadata work (#115, #128, #300, and #303).
- Docstring metadata convention work (#300) should follow multi-host and pass-boundary framing (#55 and #97), simple string docstrings (#301) should follow #300 plus evaluator, library, runtime reflection, and primitive metadata work (#3, #14, #27, #97, and #98), checked-in library adoption (#302) should follow #301, rich documentation property records (#303) should follow #300 and #301 while preserving ordinary R7RS reader behavior, manifest-backed primitive documentation (#344) should follow #98, #301, and #303 while keeping host-registered primitive documentation in the manifest with a distinct origin, and private procedure docstrings in debug views (#341) should follow #301 plus the debugger and Emacs debugger UI work (#44 and #227).
- Compiled and reference runtime documentation metadata (#304) should follow rich documentation records (#303), the R7RS and custom library reference work (#24 and #284), and the LLIR front door (#115, #116, and #120) so interpreter and compiled-runtime reflection share one Scheme-readable documentation record shape. Case-lambda clause docstring metadata (#340) should follow #303 and #304 because it needs clause-level procedure metadata rather than the initial single-string procedure docstring slot.
- Full continuation re-entry (#81) should follow the representative control feature slice (#15) before claiming complete continuation behavior in R7RS-small compliance (#19).
- Native Scheme inexact numeric ownership (#84) should follow numeric tower (#16) before claiming complete standard-library behavior in R7RS-small compliance (#19).
- Self-hostable portable numeric backend work (#350) should follow numeric tower (#16), native inexact ownership (#84), and multi-host bootstrap framing (#55), is placed in the dedicated post-current portable bootstrap ownership chunk rather than backfilled into the historical core chunk, defines which host numeric operations are semantic blockers versus temporary accelerators, and feeds the later compiled runtime ABI and allocation/GC contract (#120 and #333).
- Native Scheme character externalization parity (#83) should follow core data and writer behavior (#17) before ports, read/write, load, and eval policy (#18).
- Portable character model ownership (#349) should follow core data and writer behavior (#17), multi-host bootstrap framing (#55), and character externalization parity (#83), is placed in the dedicated post-current portable bootstrap ownership chunk rather than backfilled into the historical core chunk, and defines Agent Scheme-owned character and Unicode behavior rather than host R7RS behavior.
- Portable symbol identity ownership (#346) should follow core data and writer behavior (#17) and multi-host bootstrap framing (#55), is placed in the dedicated post-current portable bootstrap ownership chunk rather than backfilled into the historical core chunk, ensures portable reader/evaluator/macro/library code uses Agent Scheme-owned symbol datums instead of host-native symbols, and feeds the compound datum and equivalence follow-ups (#347 and #348).
- Portable compound datum heap ownership (#347) should follow core data and writer behavior (#17), multi-host bootstrap framing (#55), and portable symbol identity ownership (#346), is placed in the dedicated post-current portable bootstrap ownership chunk rather than backfilled into the historical core chunk, ensures pairs, strings, vectors, bytevectors, mutation, cycles, and shared structure are represented by Agent Scheme-owned objects rather than host-native datums, and precedes equivalence cleanup (#348).
- Portable equivalence cleanup (#348) should follow numeric tower work (#16 and #84), core data and writer behavior (#17), multi-host bootstrap framing (#55), portable symbol identity ownership (#346), and the owned compound datum heap (#347), is placed in the dedicated post-current portable bootstrap ownership chunk rather than backfilled into the historical core chunk, and replaces host `eq?`/`eqv?` fallbacks with Agent Scheme-owned value identity.
- R7RS-small compliance (#19) should remain distinct from optional SRFI support (#54). Portable bootstrap blocker follow-ups (#346 through #351) are post-core ownership work placed in a dedicated post-current chunk rather than prerequisites backfilled into the historical core chunk.
- Vendored SRFI bundle work (#54) should follow library resolution (#50) and helper self-tests (#31); import naming and R7RS-overlap shims should precede substrate libraries, SRFI 69 (#174) should follow SRFI 125 (#178), and later ergonomic libraries should wait until representative substrate chunks are stable.
- Multi-host boundaries (#55) should keep Emacs as first host without making Emacs the semantic center.
- Host-neutral search (#267) should follow #32, #28, #55, and #235; it should expose a portable agent-facing search contract while adapting host-specific providers behind the boundary.
- Emacs debugger UI (#227) should follow debugger and restart UX (#44) and preserve the portable condition and restart datum surface while adding policy-gated host restart actions.
- Agent task lifecycle and control loop design (#281) should follow the structured event channel (#21), scoped memory (#22), capability environment (#102), and model provider routing (#26) for its design slice, while staying aligned with the later provider capability domain (#223). It should define how plans (#29), rules (#58), workflow patterns (#59), skills (#38, #40, #39), transcripts (#42), collaboration UX (#52), jobs and budgets (#46, #51), providers (#26, #223), approvals and grants (#30, #43), and host capabilities compose into a running task-oriented agent, and it should prove that composition with at least one remote provider API and one local inference engine/API using shared Scheme-readable request, result, redaction, transcript, budget, cancellation, and audit datums.
- Task lifecycle records (#285) should follow #281, #21, #22, #29, and #102, and should precede the minimal runner (#286), control-loop fixtures (#287), and task persistence/resume work (#288).
- Minimal task runner work (#286) should follow #285 plus the plan, approval, grant, job, budget, and capability substrate, and should provide the first executable control-loop slice before production scheduling or provider hardening.
- Task control-loop fixtures (#287) should follow #285 and #286, then become the shared scenario corpus for task persistence (#288), provider proof fixtures (#289), collaboration UX (#52), and future task-runner conformance.
- Task persistence and resume work (#288) should follow #48, #285, #41, #42, and #49, and must revalidate authority, redaction, budgets, provider disclosure, and stale handles before a resumed task performs effects.
- Live provider CI smokes (#318, #319, and #320) should follow #26 and #69, stay focused on direct local provider routing, transport, and fail-closed behavior, and avoid turning the required live job into a provider matrix or extra model-download path.
- Remote and local provider proof fixtures (#289) should follow #281, #286, #26, #223, #49, #51, #57, and #290, and should demonstrate one remote provider API plus one local inference API through the same task lifecycle and provider datums.
- The live provider control-loop CI proof (#321) should follow #289 plus the minimal runner, fixture, provider, redaction, budget, protocol, and network substrate (#286, #287, #26, #49, #51, #57, #69, #223, and #290), and should prove one tiny live local provider call through task lifecycle records without requiring remote credentials.
- Host adapter contract conformance (#237) should follow multi-host boundaries (#55), the shared capability environment (#102), the shared backend effect path (#103), host adapter reflection (#235), and introspection conformance (#236); child conformance issues that name native CLI fixtures or process-boundary harnesses should stay after #229 through #232, and future host adapter contract issues should reference #237 for reusable conformance requirements.
- Native CLI contract work (#136) should precede broader host-compiled portable executable packaging (#270, #272, and #273), the native CLI harness slices (#229 through #232 and #360), and native CLI/daemon VCS adapter work (#280). The Gambit portable runtime executable (#273) should follow the initial `make compile` scaffolding (#270) and stay limited to portable R7RS compile/run coverage while the full host-compiled packaging path catches up. Host-compiled executable work remains distinct from the LLIR/native compiler backend (#115 through #121).
- Native CLI daemon manifest fixtures (#229) should follow the contract slice (#136); mock adapter tests (#230) should follow #229; process-boundary (#231) should follow #229 and #230; the portable terminal REPL shell (#360) should follow #231 and the policy-gated `(scheme repl)` interaction environment (#313); daemon-control (#232) should follow #229 and #230 as a sibling host-boundary validation track.
- Native CLI/daemon VCS adapter work (#280) should follow the native CLI contract (#136) and shared VCS contract (#266), reuse the portable `(agent vcs)` records and parsers, and keep read-only VCS observation distinct from mutating VCS authority (#279).
- Native CLI/daemon mutating VCS adapter work (#293) should follow #279, #280, #136, #222, #244, and #49, and prove the same mutating VCS authority boundary outside Emacs.
- Public naming migration (#61) landed as early project-frame cleanup after the architecture named Agent Scheme, and should keep later REPL, MCP, and durable API work from introducing informal identifiers.
- Rules (#58) should remain distinct from policy (#7): rules guide behavior, policy gates authority.
- Secrets/redaction (#49) should precede remote-provider routing and persistence/export work.
- Comprehensive budgets (#51) should consolidate after jobs (#46) and model provider routing (#26), while tahoma/agent-scheme#3 supplies the earliest runaway-eval guardrails.
- Budget ledger and stop receipt work (#291) should follow #51, #21, #46, and #102 so tasks, jobs, transcripts, providers, and host capabilities can explain budget exhaustion through one inspectable record.
- Outward protocol libraries (#57) should follow MCP exposure (#9), model/provider routing (#26), Hugging Face bootstrap profiles (#25), package resolution (#50), and vendored SRFI bundle tracking (#54).
